name = "obragestao-api"
main = "worker.js"
compatibility_date = "2024-01-01"

# Rota — substitua pelo seu subdomínio Cloudflare se tiver domínio próprio
# routes = [{ pattern = "api.seudominio.com.br/*", zone_name = "seudominio.com.br" }]

[vars]
# Variáveis públicas (não sensíveis) ficam aqui
APP_NAME = "ObraGestão"

# A chave da API NÃO vai aqui — vai como Secret:
# npx wrangler secret put ANTHROPIC_API_KEY
